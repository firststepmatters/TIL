import sys
input = sys.stdin.readline

N = int(input())
data = list(map(int,input().split()))
total = 0
sol = int(input())
for i in data:  
    if i == sol: 
        total += 1
print(total)

## 타 풀이 : list.count(sol) 
## 리스트 함수에 붙일수있는것에 익숙해지기
